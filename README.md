# conectando-desaparecidos
Plataforma web para centralizar, reportar y facilitar la búsqueda de personas desaparecidas

```markdown
## Instalación

Instala las dependencias necesarias para el proyecto. Utilizamos `html2canvas` para capturar el contenido de la web y `jspdf` para generar el documento PDF.

Ejecuta el siguiente comando en tu terminal:

```bash
npm install html2canvas jspdf

### Opción 2: Si prefieres ponerlo por separado
Si ya habías escrito lo anterior y solo quieres agregar esta nueva línea debajo, puedes hacerlo así:

```markdown
Para los iconos de la interfaz, también necesitarás instalar esta librería:

```bash
npm install react-icons